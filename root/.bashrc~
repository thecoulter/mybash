#!/bin/bash

BASHPATH='/root/mybash/'

# apt
alias I='sudo apt install'
alias update='apt update && apt upgrade -y'

# ssh
alias ssh='ssh -F $BASHPATH.ssh/config'
alias scp='scp -F $BASHPATH.ssh/config'
